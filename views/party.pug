include ../partials/header.pug

script.
  party = !{ JSON.stringify(party) }

section.party
  div.partyHeader
    h2= party.name
    h3 Créé par #{ party.author }, prévu le #{ party.date }

  include ../partials/form_manage_item.pug

  include ../partials/url.pug